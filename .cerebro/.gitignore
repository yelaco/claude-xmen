# Cerebro runtime state — project-specific, never committed
boulder.json
.pending-todos
upgrade-state.json
project-context.md
upgrade-cache/

# Accumulated wisdom is per-project
notepads/*/
!notepads/.gitkeep

# Keep plans and team-runs tracked — outputs of Professor X and Cyclops
# plans/ and team-runs/ are intentionally tracked
